The GoogleTrans API must be installed in python libraries.
To install or download write the following command on cmd with python open in the cmd:
pip install googletrans-py
After that it's only the work to run the code.